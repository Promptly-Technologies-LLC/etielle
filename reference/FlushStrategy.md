## FlushStrategy


Defines persistence behavior at a chunk/component boundary.


Usage

``` python
FlushStrategy()
```


## Methods

| Name | Description |
|----|----|
| [flush()](#flush) | Flush `scope_tables` using `bind_context` for relationship binding. |

------------------------------------------------------------------------


#### flush()


Flush `scope_tables` using `bind_context` for relationship binding.


Usage

``` python
flush(ctx)
```
