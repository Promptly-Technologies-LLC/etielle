## get()


Resolve a value relative to the current node using a dot-separated path


Usage

``` python
get(path)
```


(or an explicit sequence of segments). Supports list indices when an int segment is provided.
